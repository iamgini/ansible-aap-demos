---
version: 3

images:
  base_image:
    name: registry.redhat.io/ansible-automation-platform-27/ee-supported-rhel9:latest

dependencies:
  system:
    - python3-pip [platform:rpm]
    - python3-devel [platform:rpm]
    - gcc [platform:rpm]
    - pkgconf-pkg-config [platform:rpm]
    - systemd-devel [platform:rpm]
  galaxy:
    collections: []
      # - name: ansible.mcp
      # - name: ansible.utils
      # - name: ansible.scm
      # - name: ansible.eda
      # - name: ansible.controller
      # - name: ansible.platform
  python:
    - pyyaml
    - yamllint
    - molecule
    - ansible-lint

# additional_build_steps:
  # prepend_galaxy:
  #   # Uncomment for Private Automation Hub
  #   - ENV ANSIBLE_GALAXY_SERVER_LIST=pah_community,rh_certified
  #   - ARG ANSIBLE_GALAXY_SERVER_RH_CERTIFIED_TOKEN
  #   - ARG ANSIBLE_GALAXY_SERVER_PAH_COMMUNITY_TOKEN
  # append_final:
  #   # Uncomment to add custom CA certificates
  #   - COPY custom-ca.cer /etc/pki/ca-trust/source/anchors/custom-ca.cer
  #   - RUN update-ca-trust

options:
  package_manager_path: /usr/bin/microdnf
